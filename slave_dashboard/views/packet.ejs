<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Packet Details</title>
  </head>
  <body>
    <h1 class="heading">Packet Details</h1>
    <div class="packet">
      <% for(let point in packet) { %>
      <p style="display: flex; justify-content: space-around">
        <b style="width: 30%"><%= point %></b
        ><span style="width: 50%; text-wrap: wrap"><%= packet[point] %></span>
      </p>
      <% } %>
    </div>
  </body>
</html>
